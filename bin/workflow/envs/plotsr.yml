name: pandas
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - pandas=2.0.3
  - numpy=1.25.0
  - matplotlib-base>=3.3
# these dependencies are are for plotsr. notice how i'm not using conda to install plotsr?
# the plotsr scripts live in the bin/scripts folder. This is my version of plotsr
